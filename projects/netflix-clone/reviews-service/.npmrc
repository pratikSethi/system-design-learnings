# This personal project uses the PUBLIC npm registry.
# Overrides the global ~/.npmrc (which points at Amazon-internal CodeArtifact and needs a
# work auth token) for THIS folder tree only. It does not modify ~/.npmrc in any way.
registry=https://registry.npmjs.org/
